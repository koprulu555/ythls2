name: YouTube Live Playlist Updater

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 * * *'

jobs:
  update-playlist:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install requests

    - name: Run YouTube playlist generator
      run: python youtube_playlist_generator.py
      timeout-minutes: 30

    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add youtube_live.m3u
        git diff --cached --quiet || git commit -m "🔄 YouTube playlist güncellendi: $(date +'%Y-%m-%d %H:%M:%S')"
        git push
